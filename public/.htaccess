# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteCond %{REQUEST_URI} ^/api/(.*)$
#     RewriteRule ^api/(.*)$ https://api.irishrail.ie/realtime/realtime.asmx/$1 [P,L]
# </IfModule>

## Create a test page to check modules:

# Test if modules work by setting headers
<IfModule mod_headers.c>
    Header always set X-Mod-Headers-Available "yes"
</IfModule>

<IfModule mod_rewrite.c>
    Header always set X-Mod-Rewrite-Available "yes"
</IfModule>

<IfModule mod_proxy.c>
    Header always set X-Mod-Proxy-Available "yes"
</IfModule>